{
  "name": "@app/scorer",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.19.0"
  },
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx --tsconfig ./tsconfig.json --watch index.ts",
    "build:vite": "vite build",
    "check-format": "pnpm -F ft_transcendence check-format",
    "fix-format": "pnpm -F ft_transcendence fix-format",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:watch": "tsc -p tsconfig.json --noEmit --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@utils/logger": "workspace:^",
    "@utils/metrics": "workspace:^",
    "axios": "^1.12.0",
    "dotenv": "^17.2.2",
    "fastify": "^5.6.0",
    "ioredis": "^5.8.0",
    "uuid": "^13.0.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.2"
  }
}

// Additional forgiveness for paddle collisions without changing the visual mesh.
// Applied symmetrically along Z; total added thickness = 2 * PADDLE_HITBOX_PADDING_Z.
export const PADDLE_HITBOX_PADDING_Z = 0.02;

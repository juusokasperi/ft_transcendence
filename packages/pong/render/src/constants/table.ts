// packages/pong/render/src/constants/table.ts
//
// Shared measurements for the regulation table used in both rendering and host
// layout helpers. Values are expressed in meters to match the physics engine.

/** Regulation table length along the X axis (meters). */
export const TABLE_LENGTH_X = 2.74;

/** Regulation table width along the Z axis (meters). */
export const TABLE_WIDTH_Z = 1.525;

/** Table slab thickness along the Y axis (meters). */
export const TABLE_DEPTH_Y = 0.05;

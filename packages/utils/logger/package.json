{
  "name": "@utils/logger",
  "version": "0.0.0",
  "private": true,
  "description": "Logging wrapper for Pino and ECS format",
  "type": "module",
  "sideEffects": false,
  "engines": {
    "node": ">=22.19.0"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "rimraf dist && tsc -b",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist",
    "lint": "pnpm -F ft_transcendence lint",
    "check-format": "pnpm -F ft_transcendence check-format",
    "fix-format": "pnpm -F ft_transcendence fix-format"
  },
  "dependencies": {
    "@elastic/ecs-pino-format": "^1.5.0",
    "pino": "^10.0.0"
  },
  "devDependencies": {
    "fastify": "^5.6.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  },
  "peerDependencies": {
    "fastify": "^5.6.0"
  }
}

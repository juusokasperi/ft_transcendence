# Snapshot of infra
# Generated: 2025-09-10T06:01:23.425Z
# Total files: 2

===== BEGIN infra/nginx/default.conf =====
upstream fe { server frontend:5173; }
upstream be { server backend:3001; }

server {
  listen 80;
  server_name _;

  # API to Fastify
  location /api/ {
    proxy_pass http://be;
    proxy_http_version 1.1;

     # ✅ tell backend the real origin
    proxy_set_header Host               $http_host;
    proxy_set_header X-Forwarded-Proto  $scheme;
    proxy_set_header X-Forwarded-Host   $http_host;
    proxy_set_header X-Forwarded-Port   $server_port;
    proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
  }

  # Vite HMR websocket
  location /@vite {
    proxy_pass http://fe;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $http_host;
  }

  # Everything else -> Vite dev server
  location / {
    proxy_pass http://fe;
    proxy_http_version 1.1;
    proxy_set_header Host $http_host;
  }
}

===== END infra/nginx/default.conf =====


===== BEGIN infra/nginx/Dockerfile.dev (SKIPPED: non-text) =====


# Snapshot of packages/protocol
# Generated: 2025-09-10T06:01:23.443Z
# Total files: 3

===== BEGIN packages/protocol/events.ts =====
// src/shared/protocol/events.ts
import type { WallSide } from "@shared/domain/ids";

export type FrameEvents = {
  /** Wall contact point and incoming Z-speed magnitude (for FX only). */
  wallHit?: { side: WallSide; x: number; z: number; vzAbs: number };
  explode?: { x: number; z: number };
};

===== END packages/protocol/events.ts =====


===== BEGIN packages/protocol/input.ts =====
// src/shared/protocol/input.ts
export type InputIntent = {
  leftAxis: number; // -1..1 (down/up on Z)
  rightAxis: number; // -1..1
};

export const ZeroIntent: InputIntent = { leftAxis: 0, rightAxis: 0 };

===== END packages/protocol/input.ts =====


===== BEGIN packages/protocol/state.ts =====
// src/shared/protocol/state.ts
export type GameHistoryEntry = {
  gameIndex: number;
  east: number;
  west: number;
  winner: "east" | "west";
};

===== END packages/protocol/state.ts =====

